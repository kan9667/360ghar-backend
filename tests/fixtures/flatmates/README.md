# Flatmates Fixtures

Use this folder for reusable backend test fixtures: users, profiles, listings, swipes, matches, conversations, visits, payments, notifications, and moderation reports.

Fixtures should be deterministic, minimal, and safe to compose in transaction-scoped tests.
