# Flatmates API Tests

Use this folder for endpoint behavior through ASGI clients with controlled auth and database fixtures.

Each endpoint story should have a happy-path case plus auth, validation, missing resource, permission, empty result, and service-exception cases where applicable.
