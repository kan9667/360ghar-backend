# Flatmates Unit Tests

Use this folder for backend Flatmates unit tests: services, repositories, validators, permissions, event builders, and helpers.

These tests should avoid network and database dependencies unless explicitly using a fixture.
